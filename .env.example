# {{hostname}} in artifacts is resolved from this value at runtime.
# Example: uat-turkticaretbankasi.ttbank.local
HOSTNAME=uat-turkticaretbankasi.ttbank.local

HEADLESS=true
IGNORE_HTTPS_ERRORS=true
WORKERS=4
RETRIES=0
REQUEST_TIMEOUT_MS=30000
